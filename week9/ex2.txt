The sequence is in file input.txt file.
Each virtual page have an age, so that's why the pages that are not very old stays in memory.
So if we look into logs, we could see that pages 10 11 12 13 will present in memory,